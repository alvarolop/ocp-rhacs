---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Operator installation (namespace, operatorgroup, subscription)
  - operator
  # DISA STIG scan bound to the built-in "default" ScanSetting
  - scansettingbinding-stig.yaml
